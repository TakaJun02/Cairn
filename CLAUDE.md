# guidanceLLM2 開発規約

鳥海山エリア観光ガイダンスシステムの研究プロトタイプ。秋田県立大学の研究プロジェクト。

## 最重要ルール: Docs 駆動開発

**このプロジェクトでは、Docs/ 内の文書で仕様・設計を決定し、それに基づいて実装・開発に着手する。**

1. 機能の追加・変更・リファクタリングは、まず Docs/ の該当文書を作成・更新して仕様と設計を確定させる
2. 文書はユーザーのレビュー・承認を得てから実装に入る
3. 実装は承認済み文書に基づいて行う。実装中に文書との乖離が必要になったら、**先に文書を修正して承認を得る**(実装を先行させない)
4. アーキテクチャ上の重要な意思決定は Docs/adr/ に ADR (Architecture Decision Record) として記録する
5. 実装完了後、文書と実態がずれていないか確認し、ずれていれば文書を追従させる

例外: 誤字修正や明白なバグ修正など、仕様・設計に影響しない軽微な変更は文書更新を省略してよい。ただし修正に仕様の解釈が必要な場合は該当文書を確認し、必要なら文書側も直す。

文書体系の詳細は [Docs/README.md](Docs/README.md) を参照。

## 役割分担: 実装・調査は Codex に委譲する(2026-07-30 確定)

**このプロジェクトでは、実装作業と調査作業そのものを Codex に委譲する。Claude(Fable)は仕様・設計・レビューを担当する。**

| 担当 | 範囲 |
| --- | --- |
| **Claude (Fable)** | 要求・仕様・設計の決定、Docs/ の文書執筆、選択肢の比較とユーザーとの議論、**Codex への指示書の作成**、**Codex の成果物のレビューと受け入れ判断**、ADR の記録 |
| **Codex** | **実装**(コード変更・テスト作成・リファクタリング・動作確認)、**調査**(Web 調査・論文サーベイ・コードベース調査・ライブラリ比較の一次情報収集) |

### 委譲の手順

1. **委譲前に文書を確定させる。** 実装なら承認済みの設計文書(`Docs/30_design/`)、調査なら調べる項目・出力形式・制約を明記した指示。Docs 駆動開発の原則(上記)は委譲しても変わらない — **文書なしで Codex に丸投げしない**
2. `codex:rescue` スキル(または codex-rescue サブエージェント)で委譲する。**モデルと Effort は下記のルールで固定する**
3. **成果物は必ず Claude がレビューする。** 調査結果は主要な主張の一次資料を Claude 自身が確認する(サブエージェントの要約が原典と食い違う事例が実際にあった — 2026-07-30、JSAI2025 論文の結論を肯定/否定で取り違え)。実装は差分を読んで受け入れ判断する
4. レビュー結果と乖離があれば、Claude が文書を直すか、Codex に差し戻す

### 例外(Claude が直接手を動かしてよい範囲)

- Docs/ 配下の文書執筆・更新(これは Claude の担当そのもの)
- 委譲の判断に必要な事実確認(ファイルの存在確認、データ件数の確認、設定値の確認など、数コマンドで済むもの)
- 一次資料の検証(上記手順 3)
- ユーザーが明示的に Claude を指名した作業

### Codex 実行設定(必須・2026-07-30 確定)

**Codex に委譲するときは、モデル `GPT-5.6 Sol`、Effort `max` を使う。**

```
# codex:rescue / codex-rescue サブエージェント経由(通常はこちら)
--model gpt-5.6-sol            # --effort は付けない(未指定 = max)

# Codex CLI を直接叩く場合
codex exec --model gpt-5.6-sol -c model_reasoning_effort=max
```

- モデル ID は **`gpt-5.6-sol`**。`sol` という短縮エイリアスは Codex プラグインに登録がなく、そのまま API に渡って 400 になるため**必ず完全な ID で書く**
- Effort は **`max`**。ただし **`codex:rescue` 経由では `--effort max` を渡してはいけない** — プラグインの受理値は `none/minimal/low/medium/high/xhigh` のみで、`max` はバリデーションエラーになり委譲そのものが失敗する。**`--effort` を指定しなければ Sol のデフォルト `max` が適用される**(2026-07-30 実測: プラグイン経由の実行でもセッションログに `"effort":"max"` が記録される)
- したがって `xhigh` を明示するのは**ダウングレード**にあたる。指定しないのが正しい
- Effort を下げるのはユーザーの明示的な指示があるときだけ
- 適用された Effort の確認方法: `~/.codex/sessions/<年>/<月>/<日>/rollout-*.jsonl` に `"model"` と `"effort"` が記録される

### 環境メモ(この開発マシン固有)

- Codex プラグインは ChatGPT アカウント認証
- 完了待ちは `codex-companion.mjs status <job-id> --json` のポーリング。状態は `.job.status` にネストされている(`.status` を見ると常に null で監視が終わらない)

## 手段の決め方(要求と手段の分離)

- 要求文書(Docs/10)は「何ができるか」だけを定める。**実現手段は要求段階で確定しない**
- 手段は設計段階で決める。その際、**現行(旧)実装のやり方を既定値として踏襲しない**。「やりたいこと」をベースにゼロベースで選択肢を挙げ、比較・検討した上で、ユーザーと議論して決める
- 特に **POI 推薦(FR-1)と旅程プランニング(FR-2)は研究的に深い領域**(AI によるレコメンド・プランニングは現在も最先端の研究分野)。必ず「選択肢の調査・比較 → ユーザーとの議論 → 決定(ADR) → 詳細設計」の順を踏み、安易に一案へ飛びつかない
- 手段が自明でないテーマの設計文書(Docs/30_design)は、決定稿の前に「案の比較」の節を含める
- この「選択肢の調査」の**実作業は Codex に委譲する**(上記「役割分担」)。Claude は調べる項目と出力形式を指示し、返ってきた結果の一次資料を検証した上で、比較・提案として文書に書く

## 言語

- ドキュメント・コミットメッセージ・ユーザーへの応答は日本語
- コード内の識別子は英語、コメントは日本語可

## 実行環境の前提

- 生成 LLM: ホスト上の vLLM (OpenAI 互換 API)。`.env` の `Inference_server`
- 埋め込み: 別マシンの vLLM (`.env` の `Embedding_server`)。旧構成の長期記憶のみが使用しており、再設計では廃止予定 (ADR-0002)
- その他のサービスは docker compose で起動
